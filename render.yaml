services:
  - type: web
    name: acquiremock-gateway
    env: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0

      - key: DATABASE_URL
        sync: false

      - key: WEBHOOK_SECRET
        generateValue: true

      - key: RESEND_API_KEY
        sync: false

      - key: RESEND_FROM_EMAIL
        value: AcquireMock <onboarding@resend.dev>